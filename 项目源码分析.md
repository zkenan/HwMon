# HwMon 硬件监控系统 - 项目源码分析文档

## 一、项目概述

HwMon 是一个基于 C/S 架构的 Windows 硬件监控系统。客户端部署在目标 Windows 机器上，以 Windows 服务的形式运行，定时采集硬件信息并上报到服务端；服务端是 Flask Web 应用，使用 MySQL 存储数据，提供 Web 管理界面和 REST API。

---

## 二、项目结构

```
HwMon/
├── client/                          # 客户端 Python 项目
│   ├── hardware_collector.py        # 核心：硬件信息采集模块（WMI + psutil）
│   ├── service.py                   # Windows 服务 + 本地 HTTP 服务（监听端口 13301）
│   ├── config.py                    # JSON 配置管理器（ConfigManager 类）
│   ├── config.json                  # 客户端配置文件
│   ├── config.example.json          # 配置示例
│   ├── requirements.txt             # 依赖：requests, wmi, pywin32, psutil
│   ├── diagnose.py                  # 诊断工具
│   ├── install.bat                  # 一键安装脚本
│   └── deploy_package/              # 打包部署目录
│
├── server/                          # 服务端 Python 项目
│   ├── app.py                       # 核心：Flask 应用（所有 API + Web 模板渲染）
│   ├── templates/
│   │   ├── index.html               # 主界面（客户端列表、详情、告警、配置）
│   │   └── login.html               # 登录页面
│   ├── config.json                  # 服务端配置（MySQL 连接、登录密码、采集并发参数）
│   ├── requirements.txt             # 依赖：flask, pymysql, DBUtils, openpyxl, waitress
│   └── build_exe.py                 # PyInstaller 打包脚本
│
├── deploy/                          # 已打包的可执行文件
│   ├── client/                      # 客户端 exe + 运行时依赖
│   └── server/                      # 服务端 exe + 运行时依赖
│
├── 项目文档.md                        # 项目详细文档
├── README.md
├── DEPLOY.md
└── DEPLOY_STATUS.md
```

---

## 三、客户端架构

### 3.1 核心类与模块

**`hardware_collector.py` — HardwareCollector 类**

这是整个客户端的核心，负责所有硬件数据的采集。通过 `wmi.WMI()` 连接 Windows WMI 服务，配合 `psutil` 获取系统级信息。

采集项一览（共 12 项）：

| 采集方法 | 返回字段 | 数据源 |
|----------|----------|--------|
| `get_system_info()` | hostname, os_name, os_version, os_architecture, boot_time | Win32_OperatingSystem + psutil |
| `get_cpu_info()` | name, cores, threads, max_clock_speed, manufacturer | Win32_Processor |
| `get_memory_info()` | modules[], total_capacity | Win32_PhysicalMemory |
| `get_disk_info()` | model, size, serial_number, interface_type | Win32_DiskDrive |
| `get_gpu_info()` | name, adapter_ram, driver_version | Win32_VideoController |
| `get_network_info()` | mac_address, ip_addresses[], description | Win32_NetworkAdapterConfiguration |
| `get_motherboard_info()` | manufacturer, product, serial_number | Win32_BaseBoard |
| `get_bios_info()` | manufacturer, version, serial_number, release_date | Win32_BIOS |
| `get_uptime_info()` | boot_time, uptime_seconds, uptime_human | psutil.boot_time() 计算差值 |
| `get_temperature_info()` | sensors[] (name, value, sensor_type, hardware_name), source | LibreHardwareMonitor WMI / MSAcpi_ThermalZoneTemperature |
| `get_fan_info()` | sensors[], source | LibreHardwareMonitor WMI / Win32_Fan |
| `get_voltage_info()` | sensors[], source | LibreHardwareMonitor WMI / Win32_VoltageProbe |

温度/风扇/电压的采集采用两级回退策略：
1. 优先尝试 `root\LibreHardwareMonitor` WMI 命名空间（需 LibreHardwareMonitor 在后台运行）
2. 若不可用，回退到 WMI 原生类（大多数硬件上不返回有效数据）
3. 若都不可用，返回空 sensors 数组 + hint 提示信息，不影响其他采集项

**`config.py` — ConfigManager 类**

负责加载、合并、保存 JSON 配置文件。支持嵌套键访问（如 `config.get('server', 'url')`）。

配置结构：
```json
{
  "server": { "url", "timeout", "retry_times", "retry_interval" },
  "client": { "report_interval", "auto_start", "client_id", "group_name", "listen_port" },
  "logging": { "enabled", "log_file", "max_size_mb", "backup_count" },
  "advanced": {
    "collect_cpu", "collect_memory", "collect_disk", "collect_gpu",
    "collect_network", "collect_motherboard", "collect_bios",
    "collect_temperature", "collect_fan", "collect_voltage", "collect_uptime",
    "compress_data"
  }
}
```

`should_collect(component)` 方法根据 `advanced.collect_{component}` 决定是否采集某项，可单独开关每个采集项。

**`service.py` — HwMonService 类**

注册为 Windows 服务（服务名 `HwMon`），包含两个核心职责：

1. **定时上报循环**：`SvcDoRun()` 中，首次启动立即采集上报，之后每隔 `report_interval` 秒（默认 120 秒）循环采集并上报到服务端。

2. **本地 HTTP 服务**：在 `0.0.0.0:13301` 启动一个轻量 HTTP 服务（`ClientRequestHandler`），供服务端主动采集：
   - `GET /api/status` — 返回客户端在线状态
   - `POST /api/collect` — 立即执行硬件采集并上报（被服务端触发）

`collect_hardware_info(config)` 函数根据配置文件中的开关，有选择性地采集对应硬件项。

`report_to_server()` 将数据 POST 到服务端 `/api/report` 接口，上报数据包含 client_id、hostname、hardware_info、timestamp、local_ip、report_type（scheduled/on_demand）。

---

## 四、服务端架构

### 4.1 技术栈

- **Web 框架**: Flask + Flask-CORS
- **数据库**: MySQL（PyMySQL + DBUtils 连接池，maxconnections=300）
- **WSGI 服务器**: Waitress（生产环境，20 线程）/ Flask 内置（开发环境）
- **前端**: 单文件 HTML（内嵌 CSS + JavaScript），使用 fetch API 调用后端 REST API
- **导出**: openpyxl（Excel）、csv（CSV）、json

### 4.2 数据库表结构

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `clients` | 客户端注册信息 | client_id (UNIQUE), hostname, local_ip, group_id, last_report |
| `groups` | 分组 | id, name (UNIQUE), description |
| `hardware_reports` | 每次上报的完整 JSON 数据 | client_id, report_data (LONGTEXT), report_type, timestamp |
| `hardware_history` | 历史快照（保留最近10条/客户端） | client_id, cpu_info, memory_info, disk_info, gpu_info, snapshot |
| `client_baselines` | 硬件基准快照（用于变更检测） | client_id (PK), cpu_snapshot, gpu_snapshot, memory_snapshot, disk_snapshot |
| `alert_records` | 硬件变更告警记录 | client_id, alert_type, alert_detail (JSON), resolved |
| `alert_settings` | 告警监控项配置 | monitor_cpu/gpu/memory/disk/network/motherboard/bios/temperature/fan/voltage |
| `email_config` | SMTP 邮件配置 | smtp_host, smtp_port, smtp_user, smtp_password, recipients, enabled |

### 4.3 API 接口一览

**认证相关：**
- `POST /api/login` — 登录
- `POST /api/logout` — 登出
- `GET /api/check-login` — 检查登录状态

**客户端数据上报：**
- `POST /api/report` — 接收客户端上报（含基准管理 + 异步变更检测）

**客户端管理：**
- `GET /api/clients` — 客户端列表（支持分组筛选、排序）
- `GET /api/client/<id>` — 客户端详情（含最新硬件 JSON）
- `DELETE /api/clients/<id>` — 删除客户端
- `DELETE /api/clients/batch-delete` — 批量删除
- `PUT /api/clients/<id>/group` — 分配分组
- `PUT /api/clients/batch-group` — 批量分配分组

**主动采集：**
- `POST /api/collect/<id>` — 触发单个客户端采集
- `POST /api/collect/all` — 并发触发所有客户端采集（ThreadPoolExecutor，max_workers=50）

**分组管理：**
- `GET /api/groups` — 分组列表（含客户端数量统计）
- `POST /api/groups` — 创建分组
- `PUT /api/groups/<id>` — 更新分组
- `DELETE /api/groups/<id>` — 删除分组

**基准与告警：**
- `GET /api/client/<id>/baseline` — 获取基准快照
- `POST /api/client/<id>/baseline` — 手动重置基准（用最新上报数据）
- `GET /api/client/<id>/history` — 硬件历史记录（最近10条）
- `GET /api/client/<id>/alerts` — 客户端告警记录
- `GET /api/alerts` — 所有告警（支持分页、过滤 resolved 状态）
- `PUT /api/alerts/<id>` — 标记单个告警为已解决
- `PUT /api/alerts/batch-resolve` — 批量标记告警为已解决

**告警设置与邮件：**
- `GET/PUT /api/alert-settings` — 告警监控项配置
- `GET/PUT /api/email-config` — SMTP 邮件配置
- `POST /api/email-config/test` — 发送测试邮件

**数据导出：**
- `GET /api/export/csv` — 导出 CSV
- `GET /api/export/json` — 导出 JSON
- `GET /api/export/excel` — 导出 Excel（含温度/运行时间等新字段）

### 4.4 硬件变更检测机制

1. 客户端上报数据到达 `/api/report`
2. 服务端先快速更新 `clients` 表和 `hardware_reports` 表（独立事务，减少锁持有时间）
3. 硬件变更检测提交到 `hw_detection_executor` 线程池异步执行（不阻塞主流程）
4. 检测逻辑（`_check_hardware_changes`）：
   - 首次上报 → 自动创建基准快照到 `client_baselines`
   - 后续上报 → 与基准对比（通过 `compare_hardware` 函数）
   - 对比项：CPU 型号、GPU 型号、内存总容量、硬盘数量/型号、网卡描述、主板制造商/型号、BIOS 版本、温度/风扇/电压传感器名称列表
   - 检测到变更 → 记录到 `alert_records`（若告警开关开启），可选发送邮件通知

对比逻辑中，温度/风扇/电压的对比方式是**对比传感器名称列表**，而非对比具体数值（因为数值随负载变化是正常现象）。

### 4.5 前端页面（index.html）

单文件 SPA，功能区域包括：

- **统计卡片**：总客户端数、分组数量、在线客户端数
- **左侧分组栏**：全部客户端 / 未分组 / 各自定义分组，支持点击筛选
- **客户端列表表格**：主机名、IP、分组、硬件状态、上报方式、最后上报时间、在线状态、操作按钮（采集/详情/分组/删除）
- **批量操作栏**：批量采集、批量导出 Excel、批量分配分组、批量删除
- **硬件详情模态框**：
  - 硬件信息 Tab：系统、CPU、内存、硬盘、显卡、网络、主板、BIOS、运行时间、温度传感器（带颜色：>80°C 红色、>60°C 橙色、否则绿色）、风扇转速、电压
  - 历史记录 Tab：最近 10 条采集记录
  - 告警记录 Tab：硬件变更告警（支持标记已解决）
- **告警中心模态框**：未解决/已解决告警列表，支持批量标记
- **系统配置模态框**：邮件配置 Tab + 告警设置 Tab（可开关各监控项：CPU/GPU/内存/硬盘/网卡/主板/BIOS/温度/风扇/电压）
- **自动刷新**：每 30 秒自动刷新客户端列表

---

## 五、通信协议

### 客户端 → 服务端

```
POST http://<server>:5000/api/report
Content-Type: application/json

{
  "client_id": "DESKTOP-PC001",
  "hostname": "DESKTOP-PC001",
  "hardware_info": {
    "timestamp": "2026-05-20T10:30:00.000000",
    "system": { "hostname", "os_name", "os_version", "os_architecture", "boot_time" },
    "cpu": [{ "name", "cores", "threads", "max_clock_speed", "manufacturer" }],
    "memory": { "modules": [{ "capacity", "speed", "manufacturer", "part_number" }], "total_capacity" },
    "disk": [{ "model", "size", "serial_number", "interface_type" }],
    "gpu": [{ "name", "adapter_ram", "driver_version" }],
    "network": [{ "mac_address", "ip_addresses", "description" }],
    "motherboard": { "manufacturer", "product", "serial_number" },
    "bios": { "manufacturer", "version", "serial_number", "release_date" },
    "uptime": { "boot_time", "uptime_seconds", "uptime_human" },
    "temperature": { "sensors": [{ "name", "value", "sensor_type", "hardware_name" }], "source" },
    "fan": { "sensors": [...], "source" },
    "voltage": { "sensors": [...], "source" }
  },
  "timestamp": "2026-05-20T10:30:00",
  "group_name": "",
  "local_ip": "192.168.1.10",
  "report_type": "scheduled"
}
```

### 服务端 → 客户端（主动采集触发）

```
POST http://<client_ip>:13301/api/collect
Content-Type: application/json
Body: { "trigger": "server" }

Response: { "status": "success", "message": "采集并上报成功", "client_id": "DESKTOP-PC001", "timestamp": "..." }
```

---

## 六、当前代码已修复的问题

前端 `index.html` 中告警设置相关的 JavaScript 代码在本次迭代中已同步更新：

1. **`loadAlertSettings()`** — 已添加对 `monitorTemperature`、`monitorFan`、`monitorVoltage` 三个 checkbox 的赋值
2. **`resetAlertSettings()`** — 已添加对三个新 checkbox 的重置逻辑（默认关闭）
3. **告警设置表单提交** — 已在提交的 JSON 中添加 `monitor_temperature`、`monitor_fan`、`monitor_voltage` 三个字段
