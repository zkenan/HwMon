# HwMon v2.0 进程监控 + AI 研判 开发需求文档

## 一、需求背景

当前 HwMon 系统只采集硬件级别信息（CPU 型号、内存容量、硬盘型号等），缺少对**运行时进程级别**资源占用的监控能力。运维人员需要知道：哪些进程长期占用过高资源？这些异常是否需要人工介入？

本次迭代目标：
1. 客户端新增进程级资源监控能力（CPU/GPU/内存占用）
2. 当资源占用超过阈值且持续时间超过 5 分钟时，自动上报到服务端
3. 服务端新增 AI 研判模块，对上报的异常进程数据进行智能分析
4. AI 研判采用 OpenAI 兼容接口协议，支持自定义 API 配置
5. 客户端支持 Linux 和 Windows 双平台，分别打包

---

## 二、整体架构变化

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端 (Windows / Linux)                  │
│                                                                 │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │ HardwareCollector │  │ ProcessMonitor   │  │  LocalHTTP   │  │
│  │ (硬件采集,已有)   │  │ (进程监控,新增)   │  │  (:13301)    │  │
│  └────────┬─────────┘  └────────┬─────────┘  └──────────────┘  │
│           │                      │                              │
│           │   定时上报            │   阈值告警上报                │
│           ▼                      ▼                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              report_to_server() (已有)                    │  │
│  │              report_process_alert() (新增)                │  │
│  └──────────────────────────┬───────────────────────────────┘  │
└─────────────────────────────┼───────────────────────────────────┘
                              │ HTTP POST
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       服务端 (Flask + MySQL)                     │
│                                                                 │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────────────┐ │
│  │ /api/report   │  │ /api/process  │  │  AI 研判引擎         │ │
│  │ (已有)        │  │ /alert (新增) │  │  (新增)              │ │
│  └───────┬───────┘  └───────┬───────┘  └──────────┬──────────┘ │
│          │                  │                      │            │
│          ▼                  ▼                      ▼            │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────────────┐ │
│  │ clients 表    │  │ process_alert │  │  ai_analysis 表     │ │
│  │ hardware_     │  │ _records 表   │  │  ai_config 表       │ │
│  │ reports 表    │  │ (新增)        │  │  ai_hosts 表        │ │
│  └───────────────┘  └───────────────┘  │  (新增)              │ │
│                                        └─────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                   Web 前端 (index.html)                    │  │
│  │  已有: 客户端列表 / 详情 / 告警中心 / 分组管理              │  │
│  │  新增: AI 研判中心 / 进程告警列表 / AI 配置                 │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、功能需求详述

### 3.1 客户端：进程级资源监控

#### 3.1.1 采集内容

新增模块 `process_monitor.py`，独立于 `hardware_collector.py`，使用 `psutil` 采集：

| 采集项 | 字段 | 说明 |
|--------|------|------|
| 进程 CPU 占用 | `cpu_percent` | 单进程 CPU 占用百分比（跨核心合计） |
| 进程内存占用 | `memory_percent` | 单进程物理内存占用百分比 |
| 进程 GPU 占用 | `gpu_percent` | 单进程 GPU 使用率（仅 NVIDIA，需 pynvml；无 GPU 时返回 -1） |
| 进程 GPU 显存 | `gpu_memory_mb` | 单进程 GPU 显存占用（MB） |
| 进程基础信息 | `pid`, `name`, `username`, `cmdline`, `create_time` | 进程标识信息 |
| 系统级汇总 | `total_cpu_percent`, `total_memory_percent`, `total_gpu_percent` | 系统整体资源占用 |

#### 3.1.2 阈值检测逻辑

```python
# 检测规则（可配置）
THRESHOLD = {
    "cpu_percent": 90,      # CPU 占用阈值
    "memory_percent": 90,   # 内存占用阈值
    "gpu_percent": 90,      # GPU 占用阈值
    "duration_seconds": 300  # 持续时间阈值（5分钟 = 300秒）
}
```

工作流程：
1. 每 30 秒采样一次进程资源占用（`process_check_interval`，可配置）
2. 对每个进程，如果 CPU 或内存或 GPU 占用 > 90%，记录到内存中的 `high_usage_tracker` 字典
3. tracker 记录首次超过阈值的时间
4. 如果某进程持续超过阈值的时间 >= 300 秒（5分钟），触发上报
5. 上报完成后，该进程的 tracker 重置（避免重复上报）
6. 进程退出后自动清除其 tracker 记录

#### 3.1.3 上报数据格式

客户端检测到异常后，POST 到服务端新增接口：

```
POST http://<server>:5000/api/process-alert
Content-Type: application/json

{
  "client_id": "DESKTOP-PC001",
  "hostname": "DESKTOP-PC001",
  "local_ip": "192.168.1.10",
  "timestamp": "2026-05-26T14:30:00",
  "alerts": [
    {
      "pid": 1234,
      "process_name": "python.exe",
      "username": "SYSTEM",
      "cmdline": "python main.py --verbose",
      "cpu_percent": 95.2,
      "memory_percent": 12.3,
      "gpu_percent": -1,
      "gpu_memory_mb": 0,
      "threshold_type": "cpu",
      "exceeded_since": "2026-05-26T14:25:00",
      "duration_seconds": 300
    }
  ],
  "system_summary": {
    "total_cpu_percent": 87.5,
    "total_memory_percent": 72.1,
    "total_gpu_percent": -1
  }
}
```

#### 3.1.4 客户端配置项新增

在 `config.json` 中新增 `process_monitor` 配置节：

```json
{
  "process_monitor": {
    "enabled": true,
    "check_interval": 30,
    "thresholds": {
      "cpu_percent": 90,
      "memory_percent": 90,
      "gpu_percent": 90
    },
    "duration_seconds": 300,
    "ignore_processes": ["System Idle Process", "System", "idle"],
    "gpu_enabled": false
  }
}
```

---

### 3.2 服务端：进程告警接收与存储

#### 3.2.1 新增 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/process-alert` | POST | 接收客户端进程告警（无需登录，客户端上报用） |
| `/api/process-alerts` | GET | 获取所有进程告警记录（需登录，支持分页、按主机筛选） |
| `/api/process-alerts/<id>` | PUT | 标记进程告警为已处理 |
| `/api/process-alerts/batch-resolve` | PUT | 批量标记为已处理 |

#### 3.2.2 新增数据库表

**process_alert_records 表（进程告警记录）：**

```sql
CREATE TABLE IF NOT EXISTS process_alert_records (
    id INT AUTO_INCREMENT PRIMARY KEY,
    client_id VARCHAR(255) NOT NULL,
    hostname VARCHAR(255),
    local_ip VARCHAR(45),
    alert_data LONGTEXT NOT NULL COMMENT '完整告警 JSON',
    alert_count INT DEFAULT 1 COMMENT '本次告警包含的进程数量',
    resolved TINYINT DEFAULT 0,
    ai_analyzed TINYINT DEFAULT 0 COMMENT '是否已进行 AI 研判',
    ai_result LONGTEXT COMMENT 'AI 研判结果',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_client_id (client_id),
    INDEX idx_resolved (resolved),
    INDEX idx_created_at (created_at DESC),
    INDEX idx_ai_analyzed (ai_analyzed)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 3.3 服务端：AI 主机管理

#### 3.3.1 功能说明

管理员可以手动将某些主机加入 AI 研判列表。只有在此列表中的主机产生的进程告警，才会触发 AI 研判；未加入的主机，告警照常记录但不进行 AI 分析。

#### 3.3.2 新增 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/ai/hosts` | GET | 获取 AI 监控主机列表 |
| `/api/ai/hosts` | POST | 添加主机（body: `{client_id, hostname, description}`） |
| `/api/ai/hosts/<id>` | DELETE | 移除主机 |

#### 3.3.3 新增数据库表

**ai_monitored_hosts 表：**

```sql
CREATE TABLE IF NOT EXISTS ai_monitored_hosts (
    id INT AUTO_INCREMENT PRIMARY KEY,
    client_id VARCHAR(255) NOT NULL UNIQUE,
    hostname VARCHAR(255),
    description TEXT,
    enabled TINYINT DEFAULT 1,
    added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_client_id (client_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 3.4 服务端：AI 设置模块

#### 3.4.1 功能说明

独立的 AI 配置模块，支持配置 OpenAI 兼容接口参数。兼容任何实现 OpenAI Chat Completions API 的服务（OpenAI、Azure OpenAI、本地 Ollama、vLLM、DeepSeek 等）。

#### 3.4.2 新增 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/ai/config` | GET | 获取 AI 配置（api_key 脱敏返回） |
| `/api/ai/config` | PUT | 更新 AI 配置 |
| `/api/ai/config/test` | POST | 测试 AI 连通性 |

#### 3.4.3 新增数据库表

**ai_config 表：**

```sql
CREATE TABLE IF NOT EXISTS ai_config (
    id INT PRIMARY KEY,
    enabled TINYINT DEFAULT 0 COMMENT 'AI 研判总开关',
    api_base_url VARCHAR(500) DEFAULT 'https://api.openai.com/v1' COMMENT 'API 基础地址',
    api_key VARCHAR(500) DEFAULT '' COMMENT 'API Key',
    model VARCHAR(100) DEFAULT 'gpt-4o-mini' COMMENT '模型名称',
    max_tokens INT DEFAULT 2000 COMMENT '最大输出 token 数',
    temperature DECIMAL(3,2) DEFAULT 0.3 COMMENT '温度参数',
    system_prompt TEXT COMMENT '系统提示词',
    auto_analyze TINYINT DEFAULT 1 COMMENT '收到告警后自动触发 AI 分析',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 3.4.4 默认系统提示词

```
你是一位 IT 运维专家，负责分析服务器/工作站的进程资源占用告警。
请对以下进程告警数据进行分析，给出：
1. 风险等级评估（低/中/高/紧急）
2. 每个异常进程的可能原因分析
3. 建议的处理措施
4. 是否需要立即人工介入

请用中文回复，格式清晰，给出可执行的建议。
```

---

### 3.5 服务端：AI 研判引擎

#### 3.5.1 工作流程

```
进程告警到达
    │
    ├── 是否在 ai_monitored_hosts 列表中？
    │   ├── 否 → 仅存储告警，不触发 AI
    │   └── 是 → 检查 ai_config.enabled
    │       ├── 关闭 → 仅存储告警
    │       └── 开启 → 异步调用 AI 接口
    │           │
    │           ├── 构建 prompt（系统提示词 + 告警数据）
    │           ├── POST 到 api_base_url/chat/completions
    │           ├── 解析返回结果
    │           └── 存储到 process_alert_records.ai_result
```

#### 3.5.2 AI 接口调用规范

请求格式（OpenAI Chat Completions 兼容）：

```
POST {api_base_url}/chat/completions
Authorization: Bearer {api_key}
Content-Type: application/json

{
  "model": "{model}",
  "messages": [
    {"role": "system", "content": "{system_prompt}"},
    {"role": "user", "content": "{告警数据 JSON}"}
  ],
  "max_tokens": {max_tokens},
  "temperature": {temperature}
}
```

#### 3.5.3 AI 分析结果存储

分析完成后，将结果写入 `process_alert_records.ai_result` 字段，格式：

```json
{
  "risk_level": "高",
  "analysis": "详细分析文本...",
  "recommendations": ["建议1", "建议2"],
  "need_immediate_action": true,
  "model_used": "gpt-4o-mini",
  "analyzed_at": "2026-05-26T14:31:00"
}
```

---

### 3.6 前端：AI 研判中心

在现有前端 `index.html` 中新增以下 UI 区域：

1. **AI 研判中心按钮**（在工具栏，类似"告警中心"按钮）
2. **AI 研判中心模态框**，包含三个 Tab：
   - **进程告警列表 Tab**：展示进程告警记录，已 AI 研判的显示风险等级标签，未研判的显示"待分析"
   - **AI 监控主机 Tab**：添加/删除需要 AI 研判的主机，从现有客户端列表中选择
   - **AI 设置 Tab**：配置 API 地址、Key、模型、系统提示词，支持测试连通性
3. **告警详情展开**：点击告警可查看 AI 分析结果（风险等级、分析、建议）

---

### 3.7 跨平台客户端支持

#### 3.7.1 架构调整

当前客户端大量使用 `wmi` 和 `pywin32`（Windows 专属），需要重构为跨平台兼容：

**平台检测逻辑：**
```python
import platform
IS_WINDOWS = platform.system() == "Windows"
IS_LINUX = platform.system() == "Linux"
```

**硬件采集模块改造（`hardware_collector.py`）：**

| 采集项 | Windows | Linux |
|--------|---------|-------|
| 系统信息 | WMI: Win32_OperatingSystem | `platform` + `/etc/os-release` |
| CPU | WMI: Win32_Processor | `/proc/cpuinfo` + `psutil` |
| 内存 | WMI: Win32_PhysicalMemory | `psutil.virtual_memory()` + `dmidecode` |
| 硬盘 | WMI: Win32_DiskDrive | `psutil.disk_io_counters()` + `/sys/block` |
| 显卡 | WMI: Win32_VideoController | `nvidia-smi` / `lspci` |
| 网络 | WMI: Win32_NetworkAdapterConfiguration | `psutil.net_if_addrs()` |
| 主板 | WMI: Win32_BaseBoard | `/sys/class/dmi/id/` |
| BIOS | WMI: Win32_BIOS | `/sys/class/dmi/id/` |
| 温度/风扇/电压 | WMI + LibreHardwareMonitor | `/sys/class/hwmon/` + `lm-sensors` |
| 运行时间 | `psutil.boot_time()` | `psutil.boot_time()`（通用） |
| **进程监控（新增）** | `psutil`（通用） | `psutil`（通用） |

**进程监控模块（`process_monitor.py`）：**
- `psutil` 本身是跨平台的，CPU/内存监控无需改动
- GPU 监控：Windows 和 Linux 均使用 `pynvml`（NVIDIA GPU），逻辑一致

**服务启动模块改造：**
- Windows：`service.py` 使用 `pywin32` ServiceFramework（保持不变）
- Linux：新增 `service_linux.py`，使用 `systemd` 方式部署

#### 3.7.2 Linux 服务方案

**systemd 服务文件 (`hwmon.service`)：**
```ini
[Unit]
Description=HwMon Hardware Monitor Client
After=network.target

[Service]
Type=simple
ExecStart=/opt/hwmon/hwmon_client
WorkingDirectory=/opt/hwmon
Restart=always
RestartSec=30
User=root

[Install]
WantedBy=multi-user.target
```

**Linux 入口文件 (`main_linux.py`)：**
- 使用守护进程模式运行（不依赖 pywin32）
- 定时采集上报 + 本地 HTTP 服务 + 进程监控，逻辑与 Windows 版一致

#### 3.7.3 打包方案

| 平台 | 打包工具 | 输出 |
|------|----------|------|
| Windows | PyInstaller | `HardwareMonitor.exe` + 依赖 |
| Linux | PyInstaller 或 Docker | `hwmon_client` 二进制 + systemd 文件 |

**Linux 安装包结构：**
```
hwmon-linux/
├── hwmon_client              # PyInstaller 打包的二进制
├── config.json               # 配置文件
├── hwmon.service             # systemd 服务文件
└── install.sh                # 一键安装脚本
```

---

## 四、新增/修改文件清单

### 客户端

| 文件 | 操作 | 说明 |
|------|------|------|
| `client/process_monitor.py` | **新增** | 进程资源监控模块 |
| `client/hardware_collector.py` | 修改 | 重构为跨平台（条件导入 WMI / psutil 回退） |
| `client/config.py` | 修改 | 新增 `process_monitor` 配置节 |
| `client/config.json` | 修改 | 新增进程监控配置项 |
| `client/service.py` | 修改 | 集成进程监控循环 |
| `client/service_linux.py` | **新增** | Linux 守护进程入口 |
| `client/requirements.txt` | 修改 | 新增 pynvml（可选） |
| `client/requirements_linux.txt` | **新增** | Linux 依赖（去除 wmi/pywin32） |
| `client/install.sh` | **新增** | Linux 一键安装脚本 |
| `client/hwmon.service` | **新增** | systemd 服务文件 |

### 服务端

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/app.py` | 修改 | 新增进程告警 API、AI 相关 API、数据库表初始化 |
| `server/ai_analyzer.py` | **新增** | AI 研判引擎（调用 OpenAI 兼容接口） |
| `server/templates/index.html` | 修改 | 新增 AI 研判中心 UI |

---

## 五、开发顺序

1. **阶段一：客户端进程监控** → `process_monitor.py` + 阈值检测 + 上报逻辑
2. **阶段二：服务端进程告警** → 数据库表 + 接收 API + 列表展示
3. **阶段三：AI 设置模块** → `ai_config` 表 + 配置 API + 前端设置页
4. **阶段四：AI 主机管理** → `ai_monitored_hosts` 表 + 管理 API + 前端主机页
5. **阶段五：AI 研判引擎** → `ai_analyzer.py` + 自动触发逻辑 + 结果展示
6. **阶段六：跨平台适配** → 硬件采集重构 + Linux 服务 + 双平台打包
