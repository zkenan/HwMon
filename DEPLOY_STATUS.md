# 硬件监控系统 - 部署完成说明

## 问题诊断结果

```
[1] Ping:           OK（两台客户端均在线）
[2] 8888端口:       FAIL（两台均连接被拒绝 WinError 10061）
```

**根因**: 客户端运行的是旧版程序（有HTTP 500 bug），新版已修复但尚未部署到客户端。

## 服务端状态 ✓

```
[1] 主页访问:       OK (200)
[2] 客户端列表API:  OK (200)
[3] 一键采集API:    OK (200)
[4] 上报API:        OK (200)
[5] 监听地址:       0.0.0.0:5000（所有网卡）
```

## 部署步骤

### 1. 复制部署包到客户端

部署包位置: `f:/1zkenan/1xiangmu/hardware-monitor/client/deploy_package/`

包含文件:
- `HardwareMonitor.exe` (16MB) - 新版客户端
- `deploy.bat` - 一键部署脚本（需管理员权限）
- `fix_firewall.bat` - 仅修复防火墙规则

### 2. 在每台客户端执行

**win-DL (192.168.20.18)**:
1. 将部署包复制到客户端
2. 右键 `deploy.bat` → "以管理员身份运行"
3. 等待自动完成

**longxia (192.168.20.25)**:
1. 将部署包复制到客户端
2. 右键 `deploy.bat` → "以管理员身份运行"
3. 等待自动完成

### 3. 验证部署

部署完成后等待1-2分钟，刷新服务端页面：
```
http://[服务端IP]:5000
```

点击"一键采集"按钮，应显示采集成功。

## 新版功能

| 功能 | 旧版 | 新版 |
|------|------|------|
| HTTP服务 | 500错误 | 正常响应 |
| 上报间隔 | 300秒 | 120秒 |
| 服务端重启重连 | 需手动 | 自动上报 |
| 防火墙配置 | 无 | 自动配置8888端口 |

## 注意事项

1. 部署脚本需要管理员权限
2. 部署后无需重启电脑
3. 服务端已监听所有网卡(0.0.0.0)，无需额外配置
