# HwMon 硬件监控系统 - 项目文档

## 一、项目概述

HwMon 是一个 Windows 硬件监控系统，由**客户端**和**服务端**两部分组成。客户端部署在 Windows 机器上，定时采集硬件信息并上报到服务端；服务端基于 Flask + MySQL，提供 Web 管理界面，支持查看、分组管理、数据导出、硬件变更告警等功能。

## 二、项目结构

```
HwMon/
├── client/                          # 客户端（Python）
│   ├── hardware_collector.py        # 核心：硬件信息采集模块
│   ├── service.py                   # Windows 服务 + 本地 HTTP 服务
│   ├── config.py                    # JSON 配置文件管理
│   ├── config.json                  # 实际配置文件
│   ├── config.example.json          # 配置示例
│   ├── requirements.txt             # Python 依赖
│   ├── diagnose.py                  # 诊断工具
│   ├── install.bat                  # 一键安装脚本
│   └── deploy_package/              # 打包部署目录
│
├── server/                          # 服务端（Python Flask）
│   ├── app.py                       # 核心：Flask 应用（API + Web）
│   ├── app_v5.py                    # 旧版本（可忽略）
│   ├── build_exe.py                 # 服务端打包脚本
│   └── 其他迁移/测试脚本
│
├── deploy/                          # 已打包的可执行文件
│   ├── client/                      # 客户端 exe + 依赖
│   └── server/                      # 服务端 exe + 依赖
│
├── README.md                        # 项目说明
├── DEPLOY.md                        # 部署指南
└── DEPLOY_STATUS.md                 # 部署状态
```

## 三、客户端架构

### 3.1 核心模块

| 文件 | 职责 |
|------|------|
| `hardware_collector.py` | 通过 WMI + psutil 采集硬件信息 |
| `service.py` | 注册为 Windows 服务，定时上报；同时启动本地 HTTP 服务供服务端主动采集 |
| `config.py` | 读取/合并/保存 JSON 配置 |

### 3.2 客户端数据流

```
硬件采集(HardwareCollector) → collect_hardware_info() → report_to_server() → POST /api/report
                ↑                                              ↑
     服务端主动采集请求                                     定时触发（默认120秒）
     POST /api/collect → 本地HTTP服务(:13301)
```

### 3.3 本地 HTTP 服务

客户端在 `0.0.0.0:13301` 启动一个轻量 HTTP 服务，提供两个接口：

- `GET /api/status` — 返回客户端在线状态
- `POST /api/collect` — 被服务端触发，立即采集硬件信息并上报

### 3.4 当前采集的硬件信息（共 8 项）

在 `hardware_collector.py` 中，`HardwareCollector` 类通过 WMI 和 psutil 采集以下信息：

#### 1) 系统信息 (`get_system_info`)
- 主机名 (`hostname`)
- 操作系统名称 (`os_name`)
- 操作系统版本 (`os_version`)
- 系统架构 (`os_architecture`)
- 开机时间戳 (`boot_time`) — 使用 `psutil.boot_time()` 获取

#### 2) CPU (`get_cpu_info`) — WMI: `Win32_Processor`
- 型号名称 (`name`)
- 物理核心数 (`cores`)
- 逻辑线程数 (`threads`)
- 最大主频 MHz (`max_clock_speed`)
- 制造商 (`manufacturer`)

#### 3) 内存 (`get_memory_info`) — WMI: `Win32_PhysicalMemory`
- 各条内存：容量 (`capacity`)、频率 (`speed`)、制造商 (`manufacturer`)、型号 (`part_number`)
- 总容量 (`total_capacity`)

#### 4) 硬盘 (`get_disk_info`) — WMI: `Win32_DiskDrive`
- 型号 (`model`)
- 容量 bytes (`size`)
- 序列号 (`serial_number`)
- 接口类型 (`interface_type`)

#### 5) 显卡 (`get_gpu_info`) — WMI: `Win32_VideoController`
- 名称 (`name`)
- 显存 bytes (`adapter_ram`)
- 驱动版本 (`driver_version`)

#### 6) 网络 (`get_network_info`) — WMI: `Win32_NetworkAdapterConfiguration`
- MAC 地址 (`mac_address`)
- IP 地址列表 (`ip_addresses`)
- 网卡描述 (`description`)

#### 7) 主板 (`get_motherboard_info`) — WMI: `Win32_BaseBoard`
- 制造商 (`manufacturer`)
- 型号 (`product`)
- 序列号 (`serial_number`)

#### 8) BIOS (`get_bios_info`) — WMI: `Win32_BIOS`
- 制造商 (`manufacturer`)
- 版本 (`version`)
- 序列号 (`serial_number`)
- 发布日期 (`release_date`)

### 3.5 采集控制（配置项）

在 `config.json` 的 `advanced` 字段中，可单独开关每个采集项：

```json
{
  "advanced": {
    "collect_cpu": true,
    "collect_memory": true,
    "collect_disk": true,
    "collect_gpu": true,
    "collect_network": true,
    "collect_motherboard": true,
    "collect_bios": true,
    "compress_data": false
  }
}
```

`service.py` 中的 `collect_hardware_info()` 函数根据这些配置决定是否采集对应项。

### 3.6 上报数据格式

客户端每次上报的数据结构如下（由 `collect_hardware_info` 组装）：

```json
{
  "timestamp": "2026-05-20T10:30:00.000000",
  "system": {
    "hostname": "PC-001",
    "os_name": "Microsoft Windows 10 Pro",
    "os_version": "10.0.19045",
    "os_architecture": "64bit",
    "boot_time": "1716180000.0"
  },
  "cpu": [
    {
      "name": "Intel Core i7-9700 @ 3.00GHz",
      "cores": 8,
      "threads": 8,
      "max_clock_speed": 3000,
      "manufacturer": "GenuineIntel"
    }
  ],
  "memory": {
    "modules": [
      {"capacity": 8589934592, "speed": 2666, "manufacturer": "Kingston", "part_number": "..."}
    ],
    "total_capacity": 17179869184
  },
  "disk": [...],
  "gpu": [...],
  "network": [...],
  "motherboard": {...},
  "bios": {...}
}
```

## 四、服务端架构

### 4.1 技术栈

- **框架**: Flask + Flask-CORS
- **数据库**: MySQL（通过 PyMySQL + DBUtils 连接池）
- **Web 服务器**: Waitress（生产）/ Flask 内置（开发）
- **前端**: 原生 HTML/CSS/JavaScript（模板渲染）

### 4.2 核心功能

| 功能 | API 路径 | 方法 | 说明 |
|------|----------|------|------|
| 接收上报 | `/api/report` | POST | 接收客户端硬件数据 |
| 客户端列表 | `/api/clients` | GET | 查看所有客户端 |
| 客户端详情 | `/api/client/<id>` | GET | 查看某客户端硬件详情 |
| 主动采集单个 | `/api/collect/<id>` | POST | 触发某客户端立即采集 |
| 一键采集全部 | `/api/collect/all` | POST | 并发采集所有客户端 |
| 分组管理 | `/api/groups` | CRUD | 创建/编辑/删除分组 |
| 数据导出 | `/api/export/csv\|json\|excel` | GET | 导出为 CSV/JSON/Excel |
| 硬件变更告警 | 内部自动 | - | 对比基准，记录并邮件告警 |
| 告警设置 | `/api/alert-settings` | GET/PUT | 管理告警监控项 |
| 邮件配置 | `/api/email-config` | GET/PUT | SMTP 邮件通知配置 |
| 登录认证 | `/api/login` | POST | 用户名密码登录 |

### 4.3 数据库表结构

| 表名 | 用途 |
|------|------|
| `clients` | 客户端注册信息（ID、主机名、IP、分组、最后上报时间） |
| `hardware_reports` | 每次上报的完整硬件 JSON 数据 |
| `hardware_history` | 硬件关键指标历史快照（CPU/内存/硬盘/显卡） |
| `client_baselines` | 硬件基准快照，用于变更检测 |
| `groups` | 客户端分组 |
| `alert_records` | 硬件变更告警记录 |
| `alert_settings` | 告警/邮件开关及监控项配置 |
| `email_config` | SMTP 邮件通知配置 |

### 4.4 硬件变更检测机制

服务端在收到上报后，会异步执行以下流程：

1. 首次上报：自动创建硬件基准（`client_baselines`）
2. 后续上报：与基准对比 CPU 型号、GPU 型号、内存总容量、硬盘数量/型号/容量
3. 若检测到变更：记录到 `alert_records`，可选发送告警邮件
4. 基准可通过 Web 界面手动重置

## 五、通信协议

### 客户端 → 服务端

```
POST http://<server>:5000/api/report
Content-Type: application/json

{
  "client_id": "PC-001",
  "hostname": "PC-001",
  "hardware_info": { /* 完整硬件数据 */ },
  "timestamp": "2026-05-20T10:30:00",
  "group_name": "财务部",
  "local_ip": "192.168.1.10",
  "report_type": "scheduled"
}
```

### 服务端 → 客户端（主动采集）

```
POST http://<client_ip>:13301/api/collect
Content-Type: application/json

{"trigger": "server"}
```

客户端收到后立即执行硬件采集，并通过 `/api/report` 上报到服务端。

## 六、新增采集项规划

### 6.1 用户需求

需要新增以下采集项：
- **主板温度** (Motherboard Temperature)
- **风扇速度** (Fan Speed)
- **电压** (Voltage)
- **运行时间** (Uptime)

### 6.2 实现状态：已完成 ✅

已在 `hardware_collector.py` 中新增 4 个采集方法，`config.py` / `config.json` / `service.py` 均已同步更新。

| 采集项 | 方法名 | 数据来源 | 返回字段 |
|--------|--------|----------|----------|
| 运行时间 | `get_uptime_info()` | `psutil.boot_time()` 计算差值 | `boot_time`, `uptime_seconds`, `uptime_human` |
| 主板温度 | `get_temperature_info()` | 优先 LibreHardwareMonitor WMI，回退 ACPI Thermal Zone | `sensors[]`（每项含 name, value, sensor_type, hardware_name） |
| 风扇速度 | `get_fan_info()` | 优先 LibreHardwareMonitor WMI，回退 Win32_Fan | `sensors[]` |
| 电压 | `get_voltage_info()` | 优先 LibreHardwareMonitor WMI，回退 Win32_VoltageProbe | `sensors[]` |

#### 温度/风扇/电压传感器返回格式

```json
{
  "sensors": [
    {"name": "CPU Core #1", "value": 45.0, "sensor_type": "Temperature", "hardware_name": "Intel Core i7-9700"},
    {"name": "CPU Core #2", "value": 43.0, "sensor_type": "Temperature", "hardware_name": "Intel Core i7-9700"}
  ],
  "source": "LibreHardwareMonitor"
}
```

当 LibreHardwareMonitor 未运行时，`source` 字段显示 `unavailable`，并附带 `hint` 提示信息。

### 6.3 运行时间返回格式

```json
{
  "boot_time": "2026-05-19 08:30:00",
  "uptime_seconds": 93600,
  "uptime_human": "1天2小时0分钟0秒"
}
```

### 6.4 配置开关

在 `config.json` 的 `advanced` 字段中，可单独开关每个新采集项：

```json
{
  "advanced": {
    "collect_temperature": true,
    "collect_fan": true,
    "collect_voltage": true,
    "collect_uptime": true
  }
}
```

### 6.5 温度/风扇/电压的前置依赖

- **无需额外依赖即可运行** — 即使 LibreHardwareMonitor 未安装，代码会优雅降级返回空数据和提示信息
- **完整功能需安装 LibreHardwareMonitor** — 运行 LibreHardwareMonitor.exe 并启用 WMI 命名空间后，客户端自动通过 `root\LibreHardwareMonitor` 查询传感器
- WMI 原生类（`MSAcpi_ThermalZoneTemperature`、`Win32_Fan`、`Win32_VoltageProbe`）作为回退方案，但大多数硬件上不可用

## 七、关键文件说明

### `client/hardware_collector.py`（核心采集模块）

- `HardwareCollector.__init__()` — 初始化 WMI 连接
- `get_system_info()` — 采集系统信息
- `get_cpu_info()` — 采集 CPU 信息
- `get_memory_info()` — 采集内存信息
- `get_disk_info()` — 采集硬盘信息
- `get_gpu_info()` — 采集显卡信息
- `get_network_info()` — 采集网络信息
- `get_motherboard_info()` — 采集主板信息
- `get_bios_info()` — 采集 BIOS 信息
- `collect_all()` — 一次性采集所有信息（调试用）

### `client/service.py`（Windows 服务）

- `collect_hardware_info(config)` — 根据配置采集指定硬件项
- `report_to_server()` — POST 上报到服务端
- `HwMonService` — Windows 服务类，定时采集+上报循环
- `ClientRequestHandler` — 本地 HTTP 服务，处理服务端主动采集请求
- `start_local_server()` — 启动本地 HTTP 监听

### `client/config.py`（配置管理）

- `ConfigManager` — 加载/合并/保存 JSON 配置
- `should_collect(component)` — 判断是否采集某个组件
- `get_server_url()` / `get_report_interval()` — 常用配置读取

### `server/app.py`（服务端）

- `/api/report` 接收上报 → 存储 → 异步硬件变更检测
- `/api/collect/all` 并发采集所有客户端
- 硬件基准管理 + 告警记录 + 邮件通知
- 完整的 Web 管理界面（登录、分组、导出、告警设置）
