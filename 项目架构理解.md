# HwMon 硬件监控系统 - 架构理解文档

## 一、项目概述

HwMon 是一个企业级分布式硬件监控系统，采用客户端/服务器（C/S）架构，用于集中管理和监控组织内所有计算机的硬件状态。系统通过定时采集、实时告警、智能分析等方式，帮助 IT 运维人员全面掌握硬件资产状况，及时发现异常并采取措施。

### 核心价值
- **硬件资产可视化**：集中展示所有计算机的硬件配置信息
- **实时状态监控**：跟踪硬件运行状态，检测异常变化
- **智能告警分析**：通过 AI 技术自动分析进程异常，提供处理建议
- **跨平台支持**：同时支持 Windows 和 Linux 客户端
- **高并发设计**：支持 1000+ 客户端同时在线监控

## 二、系统架构

### 2.1 整体架构图
```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层 (Windows/Linux)                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │ HardwareCollector │  │ ProcessMonitor   │  │  LocalHTTP   │  │
│  │ (硬件采集模块)   │  │ (进程监控模块)   │  │  (:13301)    │  │
│  └────────┬─────────┘  └────────┬─────────┘  └──────────────┘  │
│           │                      │                              │
│           │   定时上报            │   阈值告警上报                │
│           ▼                      ▼                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              report_to_server()                          │  │
│  │              report_process_alert()                      │  │
│  └──────────────────────────┬───────────────────────────────┘  │
└─────────────────────────────┼───────────────────────────────────┘
                              │ HTTP/HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       服务端层 (Flask + MySQL)                   │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────────────┐ │
│  │ /api/report   │  │ /api/process  │  │  AI 研判引擎         │ │
│  │ (硬件上报)    │  │ /alert        │  │  (智能分析)          │ │
│  └───────┬───────┘  └───────┬───────┘  └──────────┬──────────┘ │
│          │                  │                      │            │
│          ▼                  ▼                      ▼            │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────────────┐ │
│  │ MySQL 数据库   │  │ 进程告警表    │  │  AI 配置表          │ │
│  │ (核心数据)    │  │ (告警记录)    │  │  监控主机表          │ │
│  └───────────────┘  └───────────────┘  └─────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                   Web 前端 (SPA)                           │  │
│  │  客户端管理 / 告警中心 / AI 研判 / 分组管理 / 数据导出    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 数据流
1. **硬件信息流**：客户端定时采集 → POST /api/report → 服务端存储 → Web 展示
2. **进程告警流**：客户端检测异常 → POST /api/process-alert → 服务端存储 → AI 分析
3. **主动采集流**：服务端触发 → POST /api/collect → 客户端本地 HTTP 服务 → 采集并上报
4. **告警通知流**：硬件变更检测 → 邮件告警 → 运维人员

## 三、客户端组件详解

### 3.1 核心模块
| 模块 | 文件 | 职责 | 关键特性 |
|------|------|------|----------|
| 硬件采集器 | `hardware_collector.py` | 采集 12 类硬件信息 | 跨平台（WMI/psutil/sysfs） |
| 进程监控器 | `process_monitor.py` | 监控进程资源占用 | 阈值检测 + 持续时间判断 |
| 配置管理器 | `config.py` | 管理 JSON 配置文件 | 嵌套键访问 + 默认值合并 |
| Windows 服务 | `service.py` | 注册为系统服务 | 定时上报 + 本地 HTTP 服务 |
| Linux 守护进程 | `service_linux.py` | systemd 服务管理 | 信号处理 + 守护进程模式 |

### 3.2 硬件采集详情
**采集项（12 类）：**
1. **系统信息**：主机名、OS 版本、架构、启动时间
2. **CPU**：型号、核心数、线程数、主频、制造商
3. **内存**：模块列表（容量/频率/制造商）、总容量
4. **硬盘**：型号、容量、序列号、接口类型
5. **显卡**：名称、显存、驱动版本
6. **网络**：MAC 地址、IP 地址列表、网卡描述
7. **主板**：制造商、型号、序列号
8. **BIOS**：制造商、版本、序列号、发布日期
9. **运行时间**：启动时间、运行时长（人类可读格式）
10. **温度传感器**：传感器列表（名称/值/类型/硬件名）
11. **风扇转速**：传感器列表
12. **电压**：传感器列表

**采集策略：**
- Windows：优先使用 WMI（Windows Management Instrumentation）
- Linux：使用 psutil + /sys 文件系统 + dmidecode
- 温度/风扇/电压：优先 LibreHardwareMonitor WMI，回退到系统原生接口
- 可配置开关：每个采集项可独立启用/禁用

### 3.3 进程监控机制
**监控参数：**
- 检查间隔：30 秒（可配置）
- 阈值设置：CPU > 90%，内存 > 90%，GPU > 90%
- 持续时间：超过阈值 5 分钟（300 秒）触发告警
- 忽略进程：系统空闲进程等

**工作流程：**
1. 每 30 秒采样一次所有进程资源占用
2. 检测超过阈值的进程，记录到内存追踪器
3. 进程持续超阈值时间达到 5 分钟时触发告警
4. 告警上报后重置追踪器（避免重复上报）
5. 进程退出后自动清理追踪记录

### 3.4 本地 HTTP 服务
- **监听端口**：0.0.0.0:13301
- **接口**：
  - `GET /api/status`：返回客户端在线状态
  - `POST /api/collect`：触发立即采集并上报
- **用途**：供服务端主动采集客户端硬件信息

## 四、服务端组件详解

### 4.1 技术栈
- **Web 框架**：Flask + Flask-CORS
- **数据库**：MySQL（PyMySQL + DBUtils 连接池）
- **WSGI 服务器**：Waitress（生产环境，20 线程）
- **前端**：单文件 SPA（HTML/CSS/JavaScript）
- **AI 接口**：OpenAI 兼容协议
- **导出**：openpyxl（Excel）、csv、json

### 4.2 数据库设计
**核心表结构：**
| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `clients` | 客户端注册信息 | client_id (UNIQUE), hostname, local_ip, group_id, last_report |
| `groups` | 客户端分组 | name (UNIQUE), description |
| `hardware_reports` | 完整硬件报告 | client_id, report_data (LONGTEXT), timestamp |
| `hardware_history` | 历史快照 | client_id, cpu_info, memory_info, disk_info, gpu_info, snapshot |
| `client_baselines` | 硬件基准快照 | client_id (PK), cpu/gpu/memory/disk_snapshot |
| `alert_records` | 硬件变更告警 | client_id, alert_type, alert_detail (JSON), resolved |
| `alert_settings` | 告警配置 | monitor_* 开关字段 |
| `email_config` | SMTP 邮件配置 | smtp_host, smtp_port, recipients, enabled |
| `process_alert_records` | 进程告警记录 | alert_data (JSON), ai_analyzed, ai_result |
| `ai_config` | AI 配置 | api_base_url, api_key, model, enabled |
| `ai_monitored_hosts` | AI 监控主机 | client_id, hostname, enabled |
| `probe_targets` | 探测目标 | host, port, protocol, last_status |
| `probe_alerts` | 探测告警 | target_id, alert_type, resolved |

**性能优化：**
- 连接池：最大 300 连接，最小 30，缓存 80
- 索引优化：复合索引、降序索引
- 读写分离：只读查询使用只读连接
- 异步处理：硬件变更检测在独立线程池执行

### 4.3 核心 API
**认证相关：**
- `POST /api/login`：用户登录
- `POST /api/logout`：用户登出
- `GET /api/check-login`：检查登录状态

**客户端管理：**
- `GET /api/clients`：客户端列表（支持分组筛选、排序）
- `GET /api/client/<id>`：客户端详情（含最新硬件 JSON）
- `DELETE /api/clients/<id>`：删除客户端
- `DELETE /api/clients/batch-delete`：批量删除
- `PUT /api/clients/<id>/group`：分配分组
- `PUT /api/clients/batch-group`：批量分配分组

**数据上报：**
- `POST /api/report`：接收硬件上报（含基准管理 + 异步变更检测）
- `POST /api/process-alert`：接收进程告警

**主动采集：**
- `POST /api/collect/<id>`：触发单个客户端采集
- `POST /api/collect/all`：并发触发所有客户端采集（ThreadPoolExecutor，max_workers=50）

**分组管理：**
- `GET /api/groups`：分组列表（含客户端数量统计）
- `POST /api/groups`：创建分组
- `PUT /api/groups/<id>`：更新分组
- `DELETE /api/groups/<id>`：删除分组

**告警与基准：**
- `GET /api/client/<id>/baseline`：获取基准快照
- `POST /api/client/<id>/baseline`：手动重置基准
- `GET /api/client/<id>/history`：硬件历史记录（最近10条）
- `GET /api/client/<id>/alerts`：客户端告警记录
- `GET /api/alerts`：所有告警（支持分页、过滤）
- `PUT /api/alerts/<id>`：标记告警为已解决
- `PUT /api/alerts/batch-resolve`：批量标记告警

**告警设置与邮件：**
- `GET/PUT /api/alert-settings`：告警监控项配置
- `GET/PUT /api/email-config`：SMTP 邮件配置
- `POST /api/email-config/test`：发送测试邮件

**AI 相关：**
- `GET/PUT /api/ai/config`：AI 配置管理
- `POST /api/ai/config/test`：测试 AI 连通性
- `POST /api/ai/config/models`：获取 AI 模型列表
- `GET/POST/DELETE /api/ai/hosts`：AI 监控主机管理

**主机探测：**
- `GET/POST/PUT/DELETE /api/probe/targets`：探测目标管理
- `POST /api/probe/targets/<id>/test`：测试探测目标
- `GET /api/probe/alerts`：探测告警列表

**数据导出：**
- `GET /api/export/csv`：导出 CSV
- `GET /api/export/json`：导出 JSON
- `GET /api/export/excel`：导出 Excel

### 4.4 硬件变更检测机制
1. **基准建立**：客户端首次上报时自动创建硬件基准快照
2. **变更检测**：后续上报时与基准对比（CPU/GPU型号、内存容量、硬盘数量/型号等）
3. **异步执行**：检测在独立线程池中执行，不阻塞主流程
4. **告警记录**：检测到变更时记录到 `alert_records` 表
5. **邮件通知**：可选发送邮件告警（需配置 SMTP）

### 4.5 AI 研判引擎
**工作流程：**
1. 客户端上报进程告警到 `/api/process-alert`
2. 服务端检查该主机是否在 AI 监控列表中
3. 异步调用 AI 接口分析告警数据
4. 将分析结果存储到 `process_alert_records.ai_result`
5. 前端展示风险等级、分析建议

**AI 接口规范：**
- 兼容 OpenAI Chat Completions API
- 支持自定义 API 地址、Key、模型
- 默认使用 gpt-4o-mini 模型
- 返回结构化 JSON：risk_level, analysis, recommendations, need_immediate_action

## 五、通信协议

### 5.1 客户端 → 服务端（硬件上报）
```json
POST http://<server>:5000/api/report
Content-Type: application/json

{
  "client_id": "DESKTOP-PC001",
  "hostname": "DESKTOP-PC001",
  "hardware_info": {
    "timestamp": "2026-05-20T10:30:00.000000",
    "system": { "hostname", "os_name", "os_version", "os_architecture", "boot_time" },
    "cpu": [{ "name", "cores", "threads", "max_clock_speed", "manufacturer" }],
    "memory": { "modules": [{ "capacity", "speed", "manufacturer", "part_number" }], "total_capacity" },
    "disk": [{ "model", "size", "serial_number", "interface_type" }],
    "gpu": [{ "name", "adapter_ram", "driver_version" }],
    "network": [{ "mac_address", "ip_addresses", "description" }],
    "motherboard": { "manufacturer", "product", "serial_number" },
    "bios": { "manufacturer", "version", "serial_number", "release_date" },
    "uptime": { "boot_time", "uptime_seconds", "uptime_human" },
    "temperature": { "sensors": [{ "name", "value", "sensor_type", "hardware_name" }], "source" },
    "fan": { "sensors": [...], "source" },
    "voltage": { "sensors": [...], "source" }
  },
  "timestamp": "2026-05-20T10:30:00",
  "group_name": "",
  "local_ip": "192.168.1.10",
  "report_type": "scheduled"
}
```

### 5.2 客户端 → 服务端（进程告警）
```json
POST http://<server>:5000/api/process-alert
Content-Type: application/json

{
  "client_id": "DESKTOP-PC001",
  "hostname": "DESKTOP-PC001",
  "local_ip": "192.168.1.10",
  "timestamp": "2026-05-26T14:30:00",
  "alerts": [
    {
      "pid": 1234,
      "process_name": "python.exe",
      "username": "SYSTEM",
      "cmdline": "python main.py --verbose",
      "cpu_percent": 95.2,
      "memory_percent": 12.3,
      "gpu_percent": -1,
      "gpu_memory_mb": 0,
      "threshold_type": "cpu",
      "exceeded_since": "2026-05-26T14:25:00",
      "duration_seconds": 300
    }
  ],
  "system_summary": {
    "total_cpu_percent": 87.5,
    "total_memory_percent": 72.1,
    "total_gpu_percent": -1
  }
}
```

### 5.3 服务端 → 客户端（主动采集）
```json
POST http://<client_ip>:13301/api/collect
Content-Type: application/json

{"trigger": "server"}

Response:
{
  "status": "success",
  "message": "采集并上报成功",
  "client_id": "DESKTOP-PC001",
  "timestamp": "2026-05-20T10:30:00"
}
```

## 六、部署方案

### 6.1 服务端部署
**环境要求：**
- Python 3.7+
- MySQL 5.7+
- 端口：5000（Web 服务）、3306（MySQL）

**部署步骤：**
1. 安装 Python 依赖：`pip install -r requirements.txt`
2. 配置 `config.json`（MySQL 连接、登录密码等）
3. 启动服务：`python app.py`（自动初始化数据库表）
4. 生产环境使用 Waitress：`waitress-serve --port=5000 app:app`

### 6.2 客户端部署
**Windows 部署：**
1. 安装 Python 依赖：`pip install -r requirements.txt`
2. 配置 `config.json`（服务器地址）
3. 安装为服务：`python service.py --install`
4. 或直接运行：`python service.py`

**Linux 部署：**
1. 安装 Python 依赖：`pip install -r requirements_linux.txt`
2. 配置 `config.json`
3. 安装 systemd 服务：`sudo cp hwmon.service /etc/systemd/system/`
4. 启动服务：`sudo systemctl start hwmon`

**打包部署：**
- Windows：PyInstaller 打包为 `HardwareMonitor.exe`
- Linux：PyInstaller 或 Docker 打包

### 6.3 配置文件
**客户端配置 (`config.json`)：**
```json
{
  "server": {
    "url": "http://server-ip:5000",
    "timeout": 10,
    "retry_times": 3,
    "retry_interval": 60
  },
  "client": {
    "report_interval": 120,
    "auto_start": true,
    "client_id": "",
    "group_name": "",
    "listen_port": 13301
  },
  "logging": {
    "enabled": true,
    "log_file": "client.log",
    "max_size_mb": 10,
    "backup_count": 5
  },
  "advanced": {
    "collect_cpu": true,
    "collect_memory": true,
    "collect_disk": true,
    "collect_gpu": true,
    "collect_network": true,
    "collect_motherboard": true,
    "collect_bios": true,
    "collect_temperature": true,
    "collect_fan": true,
    "collect_voltage": true,
    "collect_uptime": true,
    "compress_data": false
  },
  "process_monitor": {
    "enabled": true,
    "check_interval": 30,
    "thresholds": {
      "cpu_percent": 90,
      "memory_percent": 90,
      "gpu_percent": 90
    },
    "duration_seconds": 300,
    "ignore_processes": ["System Idle Process", "System", "idle"],
    "gpu_enabled": false
  }
}
```

**服务端配置 (`config.json`)：**
```json
{
  "database": {
    "host": "localhost",
    "port": 3306,
    "user": "root",
    "password": "your_password",
    "database": "hwmon",
    "charset": "utf8mb4"
  },
  "login": {
    "username": "admin",
    "password": "your_password"
  },
  "server": {
    "port": 5000,
    "host": "0.0.0.0"
  },
  "collect": {
    "max_workers": 50,
    "timeout": 15,
    "retry_times": 0
  }
}
```

## 七、当前状态与演进

### 7.1 已实现功能
1. **基础硬件监控**：12 类硬件信息采集
2. **进程资源监控**：CPU/内存/GPU 占用检测
3. **硬件变更检测**：自动基准对比 + 邮件告警
4. **AI 智能分析**：OpenAI 兼容接口集成
5. **跨平台支持**：Windows 和 Linux 客户端
6. **高并发设计**：连接池 + 异步处理
7. **Web 管理界面**：完整的 SPA 应用
8. **数据导出**：CSV/JSON/Excel 多格式

### 7.2 技术亮点
1. **优雅降级**：温度/风扇/电压采集采用三级回退策略
2. **异步处理**：硬件变更检测不阻塞主流程
3. **连接池优化**：支持 300 并发连接
4. **配置热更新**：每个采集项可独立开关
5. **跨平台抽象**：统一接口，平台特定实现

### 7.3 潜在改进方向
1. **性能优化**：大数据量下的查询性能
2. **安全增强**：HTTPS 支持、API 认证
3. **功能扩展**：更多硬件传感器支持
4. **UI 改进**：实时数据可视化
5. **运维工具**：批量部署、自动发现

## 八、总结

HwMon 是一个功能完善、架构清晰的企业级硬件监控系统。它通过 C/S 架构实现了硬件信息的集中管理，通过 AI 技术提供了智能分析能力，通过跨平台支持覆盖了主流操作系统。系统设计考虑了高并发、可扩展性、易维护性等企业级需求，是一个成熟的 IT 基础设施监控解决方案。

该系统特别适合中大型企业用于：
- 硬件资产盘点和管理
- 服务器/工作站状态监控
- 异常进程检测和告警
- 自动化运维支持
- IT 合规性审计

随着功能的持续完善和性能的不断优化，HwMon 有望成为企业 IT 运维的重要工具。